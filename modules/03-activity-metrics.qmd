---
title: "Module 3: Activity Metrics"
format:
  revealjs:
    theme: simple
    transition: fade
    slide-number: true
    footer: "Wearable-R: https://github.com/jhuwit/wearabler"
    logo: ../assets/jhu.png
    css: ../assets/logo.css
bibliography: ../assets/references.bib    
slide-level: 2
---


```{r setup, include = FALSE}
library(knitr)
library(tidyverse)
library(targets)
library(activerse)
options(digits.secs = 3)
knitr::opts_chunk$set(echo = FALSE)
```

```{r}
tar_load(data_gt3x)
```


```{r}
#| eval: true
#| echo: false
#| results: hide
#| cache: true
measures = acti_calculate_measures(data_gt3x, flag_data = FALSE)
```

# Minute Level Metrics

* Activity Count popular measure, describe other activity measure
* Steps
* Activity Levels - high/medium/low
* Human Activity (running, biking)
* Sleep (usually 30 second epoch)

# Activity Index: AI

@bai2016activity introduced the Activity Index (AI)

For a given window $H$, AI is calculated for a given participant is calculated using $AI_{0}$ (no baseline variance estimated as in paper):

$$
\text{AI}(t; H)_0 = \sqrt{ \frac{1}{3} \sum\limits_{m=1}^3 \sigma_{m}^2(t; H)  }
$$


which is the square root of the average variance of the 3 axes, and provides an average standard deviation. 

# Activity Index: AI

@bai2016activity indicated AI is more sensitive ROC curves for moderate and vigorous physical activities compared to AC and was shown to be more sensitive to sedentary and light activities than ENMO.

@bai2016activity indicated that the window to calculate AI was 1 second and "any aggregated AI was obtained by summing up all the adjacent 1-second AIs within that period".  Thus, to calculate AI at a minute level, we would calculate it at a second level and then add the 60 $\text{AI}_0$ values together.

# Activity Index: AI

```{r}
#| echo: false
measures |> 
  ggplot(aes(x = time, y = AI)) + geom_step()
```



# MAD {.small}

@mad recommended the Mean Amplitude Deviation (MAD): for=r a given epoch of length $m$ (5sec), and where $\text{VM}$ represents the vector magnitude of the signal, the calculation of MAD is:
$$
\text{MAD} = \frac{1}{m} \sum_{i=1}^{m} \left|\text{VM}_i- \overline{\text{VM}}_m \right|
$$
They recommended taking the mean value of MAD for summarization and recommended 5-second epochs.

# Alternative Metrics: MAD

```{r}
#| echo: false
measures |> 
  ggplot(aes(x = time, y = MAD)) + geom_step()
```

# MIMS {.smaller}
Monitor-Independent Movement Summary, an "open-source, universal acceleration summary metric that accounts for discrepancies in raw data among research and consumer devices" @mims. 

* raw data interpolated of the signal to 100Hz
* extrapolates signal for regions that have hit the maximum/minimum acceleration units 
* band-pass filters from 0.2 to 5Hz
* absolute value of the area under the curve (AUC) using a trapezoidal rule
* truncates low signal values to $0$.

# MIMS 

@karas2022comparison found a correlation of $\geq 0.97$ between AC and MIMS units.  

```{r}
#| echo: false
measures |> 
  ggplot(aes(x = time, y = MIMS_UNIT)) + geom_step()
```

# ENMO

* Euclidean Norm Minus One (ENMO)
* At raw level $\sqrt{X^2+Y^2+Z^2} - 1$, truncate
* Mean values are calculated for different windows

```{r}
#| echo: false
measures |> 
  ggplot(aes(x = time, y = ENMO_t)) + geom_step()
```


# Calculate Them All

* The `acti_calculate_measures` will run AC, AI, MIMS, and MAD
```{r}
#| eval: false
#| cache: true
measures = acti_calculate_measures(data_gt3x)
```

# Other Minute-Level Metrics

# MVPA definition

Counts per minute [@montoye2020development]:

- < 2860: sedentary, 
- 2860-3940: light activity
- $\geq$ 3941: moderate to vigorous physical activity (MVPA)

# MVPA definition

From [@sasaki2011validation]:

- < 2690: light
- 2690-6166 moderate
- 6167–9642: vigorous
- >9642: very vigorous

# MVPA definition

- `actinet`: deep learning [@acquah_2026]

# Oxwearables
https://github.com/OxWearables

Self-supervised learner 700,000 accelerometer data from UKBB: [@yuan2024self]

```{r}
#| echo: false
knitr::include_graphics("assets/oxwearables.png")
```

# Step Counts using `stepcount` {.smaller}

SSL model + fine tuning with capture 24 data set.

[@stepcount] python module ported using `reticulate`. 
```{r}
knitr::include_graphics("assets/stepcount.png")
```

```{r code_stepcount}
#| eval: false
#| echo: true
steps_gt3x = actimetrics::py_acti_calculate_stepcount(data_gt3x)
```

```{r get_stepcount}
#| echo: false
tar_load(steps_gt3x)
```

# Step Counts

```{r steps_over_time}
steps_gt3x |> ggplot(aes(x = time, y = steps)) + geom_step()
```

# Step Counts

```{r plot_steps}
steps_gt3x |> 
  ggplot(aes(x = steps)) + geom_histogram() + xlab("Number of Steps in 1min")
```

# Stepcount works pretty well

@koffman2024evaluating

> stepcount algorithm exhibited the highest F1 score (0.89 ± 0.11) and lowest MAPE (8.6 ± 9%) across all data sets and had the best, or comparable, F1 scores and MAPE in each individual data set.

# Stepcount works pretty well

@koffman2024evaluating

```{r}
knitr::include_graphics("assets/comparison_data.png")
```


# Steps Per Minute

```{r}
#| echo: false
tar_load(steps)
```

```{r plot_sc60_show}
#| eval: false
#| cache: true
steps |> 
  mutate(date = lubridate::as_date(time),
         hourtime = hms::as_hms(time)) |> 
  ggplot(aes(x = hourtime, y = steps)) + 
  geom_step() +
  facet_wrap(~ date, ncol = 1)
```


```{r plot_sc60}
#| dependson: plot_sc60_show
#| echo: false
#| out-width: "100%"
<<plot_sc60_show>>
```



# Asleep

asleep - a sleep classifier for wearable sensor data using machine learning [@yuan2024self]

https://github.com/OxWearables/asleep

```{r}
tar_load(data_proc)
tar_load(asleep)
```

# Asleep Plot

```{r plot_asleep_show}
#| eval: false
#| echo: true
#| cache: true
pred = asleep$predictions |> 
  mutate(time = ymd_hms(time, tz = "GMT"),
         date = lubridate::as_date(time),
         hourtime = hms::as_hms(time))
data_proc = data_proc |> 
  mutate(date = lubridate::as_date(time),
         hourtime = hms::as_hms(time))
data_proc |> 
  ggplot(aes(x = hourtime, y = counts)) + 
  geom_segment(
    data = pred,
    aes(x = hourtime, xend = hourtime,
        y = -Inf, yend = Inf, color = sleep_stage), alpha = 0.25) +
  geom_step() +
  facet_wrap(~ date, ncol = 1)
```
# Asleep Plot

```{r plot_asleep}
#| dependson: plot_asleep_show
#| echo: false
#| out-width: "100%"
<<plot_asleep_show>>
```

# Disclaimer

I haven't used `asleep` in publications yet and haven't delved into those that have

# Actinet

[@acquah_2026] - identifies "how much time is spent in sleep, sedentary behaviour, or doing physical activity."

https://github.com/OxWearables/actinet

```{r}
tar_load(actinet)
actinet = actinet |> 
  mutate(date = lubridate::as_date(time),
         hourtime = hms::as_hms(time)) 
actinet = actinet |> 
  janitor::clean_names()
```

# Actinet Plot

```{r plot_actinet_show}
#| eval: false
#| echo: true
#| cache: true
data_proc |> 
  ggplot(aes(x = hourtime, y = counts)) + 
  geom_segment(
    data = actinet,
    aes(x = hourtime, xend = hourtime,
        y = -Inf, yend = Inf, color = moderate_vigorous == 1), alpha = 0.25) +
  geom_step() +
  guides(colour = guide_legend(title = "MVPA")) + 
  facet_wrap(~ date, ncol = 1)
```
https://link.springer.com/article/10.1186/s12889-026-27719-0

# Actinet Plot

```{r plot_actinet}
#| dependson: plot_actinet_show
#| echo: false
#| out-width: "100%"
<<plot_actinet_show>>
```

# Disclaimer

I haven't used `asleep` in publications yet, but **have** seen people use it.


# `skdh` module

From Pfizer group: 
https://scikit-digital-health.readthedocs.io/en/latest/

```{r}
#| echo: false
knitr::include_graphics("assets/skdh.png")
```


# `wristpy` Module
[`wristpy`](https://github.com/childmindresearch/wristpy) - python package for wrist-worn accelerometer data processing from Child Mind Institute

```{r}
#| echo: false
knitr::include_graphics("assets/wristpy.png")
```



# Technical Stuff: Conda/Environments

- Lots of Python modules
- If using Python, you know `conda`
- If using R, Python link using `reticulate` package
- Multiple `conda` environments may be needed
- package/module A needs numpy <= 2.11 and package B needs numpy > 2.30
- "Switching" conda environments **within** a script/session is a hassle


# Summary

- Many ways to characterize activity
- Many ways to characterize behavior
- Deep learning is moving forward with this

# References
